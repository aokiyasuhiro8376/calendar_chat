<%# <p id="notice"><%= notice %></p> 
<div class = 'wrapper'>
  <%= render 'welcome' %>
  <%= render 'sub-bar' %>
</div>

<div>
  <ul>
    <% @rooms.each do |room| %>
      <li><%= link_to "ROOM#{room.id}", room_path(@user.room_id) %></li>
    <% end %>
  </ul>
</div>